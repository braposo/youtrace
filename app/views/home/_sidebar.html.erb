<% if !params[:id].nil? && logged_in? && !if_own?(params[:id]) && !current_user.is_following?(params[:id]) -%>
    <%= link_to "Subscribe", user_subscribe_path(:id => current_user, :sub_id => params[:id]) %> user.
<% end -%>